services:
  - type: web
    name: quote-generator
    env: php
    plan: free
    buildCommand: ""
    startCommand: "php -S 0.0.0.0:$PORT -t ."
    envVars:
      - key: GROQ_API_KEY
        sync: false
